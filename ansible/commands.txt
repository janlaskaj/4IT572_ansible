sudo yum install git

pip3 install ansible==4.0
pip3 install boto boto3

scp -i devops.pem  devops.pem ec2-user@ec2-3-83-178-16.compute-1.amazonaws.com:~/

cd do adresáře ansible
ansible-vault create group_vars/all/aws.yml
ansible-vault edit group_vars/all/aws.yml

formát pro klíče:
access_key: ---
secret_key: ---
session_token: ---

run:
ansible-playbook ec2_deploy.yml --user ec2-user --key-file ../devops.pem --ask-vault-pass